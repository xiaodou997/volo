{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Least-privilege capability for the trusted main window. Filesystem, shell, clipboard, notification and dialog access go through Volo's Rust commands and PermissionEngine instead of direct Tauri plugin IPC.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "updater:default",
    "process:allow-restart"
  ]
}
